- @tasks and @tasks.each do |task|
	= link_to "<div class=\"list-group-item-heading\">#{task.title}</div><div id=\"task-#{task.id}\" class=\"task-item list-group-item-text\">#{task.id}</div>".html_safe, task_path(task.id), class: 'list-group-item', remote: true