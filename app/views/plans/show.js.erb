if (this.lastSelectedPlan != $('#plan-list .active').attr('id') ) {
	var $tasks = $("<%= j( render(:partial => 'elements/task_list') ) %>");
	$('#task-list').html($tasks);
}

this.lastSelectedPlan = $('#plan-list .active').attr('id');